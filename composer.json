{
  "name": "forix/debug-tool",
  "description": "Debug (for internal uses only)",
  "type": "magento2-lib",
  "require": {
    "php": ">=5.3.0"
  },
  "autoload": {
    "classmap": [
      "./Tool/classes/"
    ]
  }
}

